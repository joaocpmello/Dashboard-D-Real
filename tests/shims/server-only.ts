// Stub do módulo virtual "server-only" do Next.js.
// Em produção, o Next substitui por um arquivo que falha se importado no bundle do client.
// Em testes (Node puro), basta um módulo vazio.
export {};
